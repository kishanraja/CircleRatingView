import XCTest
@testable import CircleRatingView

final class CircleRatingViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CircleRatingView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
